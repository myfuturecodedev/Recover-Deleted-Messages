<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Fills inner background panel with your soft yellow color token -->
    <solid android:color="@color/reminder_bg_yellow" />
    <!-- Draws the soft orange contour frame border line segment around the container -->
    <stroke
        android:width="1dp"
        android:color="@color/reminder_border_yellow" />
    <corners android:radius="@dimen/_16sdp" />
</shape>